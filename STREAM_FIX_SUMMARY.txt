🔧 VIDEO STREAM FIX APPLIED

PROBLEM:
- Video preview was black during recording
- Stream was created but never attached to video element
- VideoElement component had no way to receive the stream

SOLUTION:
1. Added stream state management to useMediaStream hook
2. Exposed currentStream through the hook chain:
   - useMediaStream → useVideoRecording → useRecorderLogic
3. Updated VideoElement to accept and attach stream prop
4. Passed stream down through component tree:
   - DesktopVideoRecorder → VideoPreviewContainer → VideoPreview → VideoElement
   - MobileVideoRecorder → VideoPreview → VideoElement

CHANGES MADE:
✅ useMediaStream.ts - Added currentStream state
✅ useVideoRecording.ts - Exposed currentStream
✅ useRecorderLogic.ts - Passed currentStream through
✅ VideoElement.tsx - Added stream prop and attachment logic
✅ VideoPreview.tsx - Added currentStream prop
✅ VideoPreviewContainer.tsx - Passed currentStream
✅ DesktopVideoRecorder.tsx - Passed currentStream
✅ MobileVideoRecorder.tsx - Passed currentStream

RESULT:
- Video preview now shows live camera feed during recording
- Stream properly attached when recording starts
- Stream cleared when recording stops
